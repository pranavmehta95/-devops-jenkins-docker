# TODO

- [ ] Confirm the intended user task (e.g., fix failing build/tests, improve Jenkins pipeline, add endpoint, etc.).
- [ ] If the goal is CI reliability: review Jenkinsfile + Dockerfile + application endpoints.
- [ ] If the goal is test correctness: compare controller endpoints with HelloControllerTest expectations.
- [ ] Run `mvn test` and inspect surefire reports for failures (if any).
- [ ] Make minimal code/config changes needed to satisfy the task.
- [ ] Run tests again and verify Docker image build (if applicable).

